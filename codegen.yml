schema: "src/**/*.graphql"
require:
  - ts-node/register
generates:
  src/types/types.d.ts:
    plugins:
      - typescript-mongodb
      - typescript
      - typescript-resolvers
    config:
      noSchemaStitching: true
