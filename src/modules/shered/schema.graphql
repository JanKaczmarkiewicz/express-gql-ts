directive @isAuthenticated on QUERY | FIELD
directive @hasRole(scope: [String]) on QUERY | FIELD

type User @entity(additionalFields: [{ path: "password", type: "string" }]) {
  _id: String! @id
  username: String! @column
  email: String! @column
  confirmed: Boolean! @column
}
